{
  "name": "Python Codespace",
  "dockerFile": "..Docker/Dockerfile",
  "context": ".",
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "zsh"
      },
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter"
        // Add other extensions if needed
      ]
    }
  },
  "forwardPorts": [5000],
  "portsAttributes": {
    "5000": {
      "label": "Application",
      "onAutoForward": "openBrowser"
    }
  },
  "postCreateCommand": "pip install -r ../.Docker/requirements.txt && uvicorn main:app --host 0.0.0.0 --port 5000"
}
