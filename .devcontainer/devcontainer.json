{
  "name": "Python Codespace",
  "dockerFile": "../Dockerfile",
  "context": ".",
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "zsh"
      },
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter"
        // Add other extensions if needed
      ]
    }
  },
  "forwardPorts": [5000],
  "portsAttributes": {
    "5000": {
      "label": "Application",
      "onAutoForward": "openBrowser"
    }
  },
  "postCreateCommand": "pip install fastapi==0.104.1 numpy==1.26.2 uvicorn==0.15.0 pandas==1.3.3 plotly==5.3.1 && uvicorn main:app --host 0.0.0.0 --port 5000"
}
